# OWLS Flutter package
Keep core types independent of dart:io and WebView plugins; expose separate entry libraries.
A native byte cache is not a WebView browser cache. Transfer requires an explicit platform transport.
Never evaluate caller-provided JavaScript. Preserve fixed document checks and completion acknowledgements.
Read ~/codes/AGENTS.md or .ores/agents/AGENTS.md when available. Never commit .ores/.

